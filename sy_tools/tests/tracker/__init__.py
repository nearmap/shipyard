"""Tests for the tracker adapters. One module per adapter, mirroring `sy_tools/tracker/`.

These modules are the one place outside `sy_tools/tracker/` where tracker-native vocabulary is legal:
`sy_tools/tests/test_tracker_seam.py` exempts exactly `test_*.py` under this directory, nothing else.
"""
